class Core {
    // #id;
    // #created_at;
    // #update_at;

    // constructor(config) {
    //     this.id = config.id;
    //     this.created_at = config.created_at;
    //     this.updated_at = config.updated_at;
    // }

    // static getAll() {
    //     const results = `SELECT * FROM "get_${this.tablename}"`

    //     if(results.rows?.length > 0) {
    //             const list = [];
    //                 rows = results.rows;

    //             for(const row of rows) {
    //                 list.push(new this(row));
    //             }
    //     }

    // }
}

module.exports = Core;
